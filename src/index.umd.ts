// this file is resolved and exported as a minified snippet for CDN
import streamsql from './index'

// @ts-ignore
!(function(window, document, navigator) { window.streamsql = window.streamsql || streamsql; })(window, document, navigator);
